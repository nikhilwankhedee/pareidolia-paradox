# Results schema

Each result row contains `experiment`, `split`, `n`, `balanced_accuracy`, `roc_auc`, `log_loss`, `threshold`, and `artifact`. Predictions use columns `image_id`, `prediction`, `experiment` and preserve metadata order.
